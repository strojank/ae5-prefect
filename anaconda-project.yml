name: Prefect
description: A simple Prefect test with Dask execution
commands:
  default:
    unix: bash prefect_run.sh
env_specs:
  default:
    channels:
    - defaults
    - conda-forge
    packages:
    - prefect
    - dask
